import 'package:flutter/material.dart';

class MobileCreateEditBookPage extends StatelessWidget {
  const MobileCreateEditBookPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}

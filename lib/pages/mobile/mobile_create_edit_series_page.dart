import 'package:flutter/material.dart';

class MobileCreateEditSeriesPage extends StatelessWidget {
  const MobileCreateEditSeriesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.amber,
    );
  }
}

import 'package:flutter/material.dart';

class MobileNotesPage extends StatelessWidget {
  const MobileNotesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('notes');
  }
}
